<div id="location">

	<h1>I am here... <span id="location_url"></span></h1>

	<div>
		<%= label_tag :name, @location.name,  :class => "text_field huge" %>
        <ul>
        <li><%= @location.description %> </li>
        <li><%= @location.latitude %></li>
        <li><%= @location.longitude %></li>
        </ul>
	</div>
		
</div>

<div id="map_container" style="margin-top: 20px;">
	<%= @map.div %>
</div>